{
  "name": "pnp-web",
  "version": "1.0.0",
  "private": true,
  "description": "Parachutes and Promises band website",
  "scripts": {
    "lint:html": "htmlhint index.html",
    "lint:css": "stylelint 'css/**/*.css'",
    "validate": "html-validate index.html",
    "lint": "npm run lint:html && npm run lint:css && npm run validate",
    "test": "npm run lint"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "stylelint": "^16.12.0",
    "stylelint-config-standard": "^37.0.0",
    "html-validate": "^9.4.0"
  }
}
